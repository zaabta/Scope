import { View } from "react-native"

const Background = ({children}) =>{
    return(
        <View
        style={{
            justifyContent: "center",
            alignContent: "center",
            color: "white",
            background: "blach"
        }}
        >

        </View>
    )
}